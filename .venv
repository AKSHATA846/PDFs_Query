# Create a virtual environment
python -m venv venv

# Activate the virtual environment
source venv/bin/activate

# Install pypdfdirectoryloader (assuming it's available on PyPI)
pip install pypdfdirectoryloader
